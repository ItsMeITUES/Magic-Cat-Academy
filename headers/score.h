#ifndef __SCORE
#define __SCORE

#include "animation.h"

#include <string>

void renderScore(int& totalScore, std::string& scoreString, LTexture& scoreText, SDL_Renderer* gRenderer);

#endif // __SCORE
